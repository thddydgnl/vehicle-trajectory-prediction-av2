model:
  name: diffusion_direct_tune_c
  architecture: diffusion_direct
  input_dim: 6
  pred_len: 30
  trajectory_dim: 60
  cond_dim: 64
  hidden_dim: 128
  diffusion_steps: 1000
  sampling_steps: 100
  beta_start: 0.0001
  beta_end: 0.02
  num_samples: 16

training:
  batch_size: 32
  epochs: 15
  learning_rate: 0.0001
  weight_decay: 0.0001
  loss: noise_mse
  gradient_clip: 1.0
  early_stopping_patience: 8
  selection_metric: minADE
  validation_num_samples: 8
  validation_sampling_steps: 40
  validation_seed: 1234
  seed: 42
  device: cuda
  num_workers: 0
  out_dir: D:/runs/vehicle_trajectory_project/full_long_tuning
